<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/main.css">
  <title>Music Player</title>
</head>
<body>
  <h1>Music Player</h1>
  <p>Showing Spotify track results. Use the photo-based emotion flow to get suggestions.</p>
  <ul>
    <% musicTracks.forEach(track => { %>
      <li>
        <%= track.title %> - <%= track.artist %>
        <% if (track.preview_url) { %>
          <audio controls>
            <source src="<%= track.preview_url %>" type="audio/mpeg">
            Your browser does not support the audio element.
          </audio>
        <% } else if (track.id) { %>
          <a href="/music/<%= track.id %>" target="_blank">Open on Spotify</a>
        <% } %>
      </li>
    <% }) %>
  </ul>
  <div class="emotion">
    <!-- <a href="/record" class="record-button">Emotion Based Songs Video</a> -->

    <a href="/photo" class="record-button">Emotion Based Songs</a>

  </div>
</body>
</html>
